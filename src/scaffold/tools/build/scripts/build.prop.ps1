
properties {
    $projectDirs = @("$codebaseRoot\src", "$codebaseRoot\test") 
    $extraProjectOutputs = @()
    # $majorVersion = "1.0"
    # $buildConfiguration = "Debug"
    # $deployToolsDir = "$codebaseRoot\build\tools\deploy-tools"
    # $nuget = "$deployToolsDir\nudeploy\nuget.exe"
    # $xunitRunner = "$codebaseRoot\build\tools\XunitRunner\xunit.console.clr4.exe"
    # $testAssemblies = "$codebaseRoot\test\myVisas-test-unit\bin\myVisas-test-unit.dll"
    # $packageDir = "$codebaseRoot\tmp\nupkgs"    
    # $reportsDir = "$codebaseRoot\reports"
    # $packageName = $packageName
    
}